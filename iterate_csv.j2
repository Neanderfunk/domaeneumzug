---
router:
  hosts:
{% for item in csvfile.split("\n") %}
{%     set list = item.split(",") %}
    {{ list[1]|trim() }}:
      ansible_host: '{{ list[2]|trim() }}'
      ansible_user: root
      domaene: '{{ list[0]|trim() }}'

{% endfor %}
